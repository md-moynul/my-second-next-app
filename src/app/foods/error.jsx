'use client'

const error = () => {
    return (
        <div>
            <h1 className="text-5xl text-error">There are something unexpected 40444</h1>
        </div>
    );
};

export default error;